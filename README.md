# Data_preprocessing

In this assignment firstly clean the data for age to fill with median and city to fill with mode and removes duplicated rows using df.drop_duplicates().

And

One-hot-encoding  ====>  create separate columns for each city.
Label encoding =======> Converts Male/Female into numbers (0,1).

Task 3:

Scaling Numerical Features

Columns to scale age, annual_income

Min-Max Scalling

Standard Scalling
